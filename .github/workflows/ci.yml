name: ID

on: [push, pull_request]

jobs:
  ci:
    runs-on: ubuntu-18.04

    steps:
    - uses: actions/checkout@master

    - name: Install dependencies
      run: docker-compose run --rm ember npm install

    - name: Runs code QA and tests
      run: ./ember test
