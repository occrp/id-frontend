<table class="statsTable">
  <thead>
    <tr>
      <th class="statsTable-main">Staff</th>
      <th class="t-right statsTable-cell">Rating</th>
      <th class="t-right statsTable-cell">{{t 'pods.reporting.columns.total'}}</th>
    </tr>
  </thead>
  <tbody>
    {{#each model as |stat|}}
      <tr>
        <td>
          {{stat.responder.firstName}} {{stat.responder.lastName}}
        </td>
        <td class="t-right">
          <strong>
            {{stat.ratings}}
          </strong>
        </td>
        <td class="t-right">
          <span class="t-danger">
            {{stat.count}}
          </span>
        </td>
      </tr>
    {{/each}}
  </tbody>
</table>
