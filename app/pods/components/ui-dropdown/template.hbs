{{yield (hash
  trigger=(component 'ui-dropdown-trigger'
    uuid=(concat "dd-" elementId)
    isOpen=isOpen
    open=(action (mut isOpen) true)
  )
  body=(component 'ui-dropdown-body'
    uuid=(concat "dd-" elementId)
    isOpen=isOpen
    close=(action (mut isOpen) false)
  )
  close=(action (mut isOpen) false)
  open=(action (mut isOpen) true)
)}}

